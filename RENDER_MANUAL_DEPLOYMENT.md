# دليل النشر اليدوي على Render.com

هذا الدليل يشرح خطوات نشر تطبيق ButterBakery على منصة Render.com بطريقة مفصلة. تناسب هذه الطريقة المستخدمين الذين يفضلون فهم العملية بالتفصيل.

## المتطلبات الأساسية

1. حساب على [Render.com](https://render.com)
2. ملف الضغط `butterbakery-render-deployment-v2.zip` (موجود في مجلد المشروع)
3. معرفة أساسية بأوامر Terminal أو Command Prompt (اختياري)

## مقدمة

Render.com هي منصة استضافة حديثة توفر طريقة سهلة لنشر التطبيقات الويب. تتميز بسهولة الاستخدام والتكامل مع قواعد البيانات.

## خطوات النشر

### 1. تحضير ملفات المشروع

لدينا خياران لتحضير الملفات:

#### أ) استخدام ملف الضغط الجاهز:
1. قم بتنزيل ملف الضغط `butterbakery-render-deployment-v2.zip` من مجلد المشروع إلى جهازك.
2. لا تحتاج إلى فك ضغط الملف - ستقوم برفعه كما هو إلى Render.com.

#### ب) تحضير الملفات يدويًا (اختياري للمستخدمين المتقدمين):
1. انسخ الملفات التالية إلى مجلد جديد:
   - مجلد `client`
   - مجلد `server`
   - مجلد `shared`
   - مجلد `public`
   - الملفات: `package.json`, `package-lock.json`, `start.js`, `drizzle.config.ts`, `tsconfig.json`, `vite.config.ts`, `tailwind.config.ts`, `postcss.config.js`
2. قم بإنشاء ملف `.gitignore` وأضف فيه:
   ```
   node_modules/
   .env
   .DS_Store
   dist/
   *.log
   ```
3. اضغط المجلد كملف `.zip`.

### 2. إنشاء قاعدة بيانات PostgreSQL

1. قم بتسجيل الدخول إلى [لوحة تحكم Render.com](https://dashboard.render.com/)
2. في القائمة الجانبية، انقر على "New" واختر "PostgreSQL"
3. أدخل المعلومات التالية:
   - **Name**: `butterbakery-db` (أو أي اسم تفضله)
   - **Database**: `butterbakery`
   - **User**: `butterbakery_user`
   - **Region**: اختر المنطقة الأقرب لمستخدميك
   - **Instance Type**: يمكنك البدء بالخطة المجانية، ثم الترقية لاحقًا حسب الحاجة
   - **Storage**: اختر 1 GB للبداية (كافٍ للاستخدام الأولي)

4. انقر على "Create Database"
5. ستبدأ عملية إنشاء قاعدة البيانات. انتظر حتى تكتمل العملية (قد تستغرق بضع دقائق)
6. بعد الاكتمال، ستظهر صفحة تفاصيل قاعدة البيانات
7. قم بنسخ عنوان "Internal Database URL" (ستحتاجه لاحقًا)

![صورة تظهر عنوان الاتصال بقاعدة البيانات](https://render.com/static/db-connection-string.png)

### 3. إنشاء خدمة ويب

1. انتقل إلى لوحة تحكم Render.com الرئيسية
2. انقر على "New" واختر "Web Service"
3. اختر "Deploy from a zip file" من خيارات النشر المتاحة
4. أدخل التفاصيل التالية:
   - **Name**: `butterbakery-app` (أو أي اسم تفضله)
   - **Environment**: Node
   - **Region**: اختر نفس المنطقة التي اخترتها لقاعدة البيانات
   - **Instance Type**: يمكنك البدء بالخطة المجانية، ثم الترقية لاحقًا حسب الحاجة

5. في قسم "Build & Deploy"، أدخل:
   - **Build Command**: `npm install`
   - **Start Command**: `node start.js`

6. في قسم "Environment Variables"، انقر على "Add Environment Variable" وأضف المتغيرات التالية:
   - `NODE_ENV`: `production`
   - `PORT`: `10000`
   - `DATABASE_URL`: [عنوان الاتصال بقاعدة البيانات الذي نسخته سابقًا]

7. في قسم "Deploy from a zip file"، قم برفع ملف `butterbakery-render-deployment-v2.zip`
8. انقر على "Create Web Service"

### 4. مراقبة عملية النشر

1. بعد إنشاء الخدمة، ستتم إعادة توجيهك إلى صفحة مراقبة النشر
2. ستظهر لك حالة البناء والنشر في الوقت الفعلي
3. يمكنك متابعة السجلات (Logs) لمعرفة تفاصيل عملية البناء
4. ستمر العملية بالمراحل التالية:
   - Build: تثبيت التبعيات
   - Deploy: تكوين الخدمة
   - Live: الخدمة جاهزة للاستخدام

5. عند اكتمال العملية، ستظهر رسالة "Your service is live 🎉"
6. انقر على الرابط المعروض للوصول إلى التطبيق

### 5. تهيئة قاعدة البيانات (تلقائيًا)

- عند تشغيل التطبيق للمرة الأولى، سيقوم تلقائيًا بإنشاء جداول قاعدة البيانات اللازمة
- سيتم أيضًا إضافة بيانات تجريبية أولية، بما في ذلك حساب المدير الافتراضي

### 6. تسجيل الدخول إلى التطبيق

1. في صفحة تسجيل الدخول، استخدم بيانات الاعتماد الافتراضية:
   - **اسم المستخدم**: `admin`
   - **كلمة المرور**: `admin`

2. بعد تسجيل الدخول، ستتمكن من الوصول إلى لوحة التحكم الرئيسية
3. قم فورًا بتغيير كلمة المرور الافتراضية من خلال الانتقال إلى صفحة الملف الشخصي

## إعدادات متقدمة (اختياري)

### إعداد النطاق المخصص (Custom Domain)

1. اذهب إلى صفحة تفاصيل خدمة الويب الخاصة بك
2. انقر على علامة التبويب "Settings"
3. انتقل إلى قسم "Custom Domain"
4. اتبع التعليمات لإضافة نطاقك المخصص

### إعداد شهادة SSL

- Render.com يوفر تلقائيًا شهادات SSL مجانية لجميع الخدمات، بما في ذلك النطاقات المخصصة
- لا تحتاج إلى إعداد يدوي لهذه الخاصية

### إعداد المراقبة والتنبيهات

1. في صفحة تفاصيل الخدمة، انقر على علامة التبويب "Metrics"
2. يمكنك مراقبة استخدام الموارد وأداء التطبيق
3. لإعداد التنبيهات، انتقل إلى علامة التبويب "Alerts" وقم بتكوين القواعد حسب احتياجاتك

## صيانة التطبيق

### مراقبة السجلات (Logs)

1. في صفحة تفاصيل الخدمة، انقر على علامة التبويب "Logs"
2. يمكنك عرض سجلات التطبيق المباشرة وسجلات البناء السابقة
3. يمكنك البحث في السجلات وتصفيتها حسب الحاجة

### إعادة تشغيل الخدمة

1. في صفحة تفاصيل الخدمة، انقر على زر "Manual Deploy" ثم "Restart Service"
2. سيتم إعادة تشغيل الخدمة دون إعادة البناء

### تحديث التطبيق

لتحديث التطبيق إلى إصدار جديد:

1. انتقل إلى صفحة تفاصيل الخدمة
2. انقر على علامة التبويب "Settings"
3. انتقل إلى قسم "Deploy from a zip file"
4. ارفع ملف الضغط الجديد
5. انقر على "Save Changes"

## استكشاف الأخطاء وإصلاحها

### خطأ في الاتصال بقاعدة البيانات

1. تأكد من أنك استخدمت عنوان "Internal Database URL" وليس "External Database URL"
2. تحقق من أن متغير البيئة `DATABASE_URL` مكتوب بشكل صحيح
3. تأكد من أن قاعدة البيانات قيد التشغيل وتعمل بشكل صحيح

### مشاكل في البناء

1. تحقق من سجلات البناء في علامة التبويب "Logs"
2. تأكد من أن أمر البناء `npm install` مكتوب بشكل صحيح
3. تأكد من أن ملف الضغط يحتوي على جميع الملفات اللازمة

### مشاكل في التشغيل

1. تحقق من سجلات التشغيل في علامة التبويب "Logs"
2. تأكد من أن أمر البدء `node start.js` مكتوب بشكل صحيح
3. تأكد من قيمة متغير `PORT` أنها `10000`

### بطء في التحميل الأولي

- هذا طبيعي في الخطة المجانية من Render.com، حيث يتم إيقاف الخدمة بعد فترة من عدم النشاط
- عند الوصول الأول بعد فترة من عدم النشاط، قد يستغرق تشغيل الخدمة بضع ثوان

## النسخ الاحتياطي واستعادة البيانات

### النسخ الاحتياطي التلقائي

- Render.com يقوم تلقائيًا بعمل نسخ احتياطية يومية لقواعد البيانات
- النسخ الاحتياطية متوفرة لمدة 7 أيام في الخطة المجانية

### استعادة النسخ الاحتياطي

1. انتقل إلى صفحة تفاصيل قاعدة البيانات
2. انقر على علامة التبويب "Backups"
3. حدد النسخة الاحتياطية التي تريد استعادتها
4. انقر على "Restore"

## التوسع والترقية

عندما يزداد استخدام التطبيق لديك، يمكنك:

1. ترقية خطة قاعدة البيانات للحصول على مساحة أكبر وأداء أفضل
2. ترقية خطة خدمة الويب للحصول على موارد أكثر وزمن تشغيل أطول
3. إضافة خدمات إضافية مثل خدمات المراقبة والتنبيه المتقدمة

---

**ملاحظة هامة**: تم إعداد هذا الدليل في مارس 2025. قد تتغير بعض تفاصيل واجهة Render.com بمرور الوقت، ولكن المفاهيم الأساسية تظل كما هي.